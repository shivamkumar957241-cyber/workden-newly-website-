// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // You can keep any other existing config here (e.g., reactStrictMode: true)
};
module.exports = nextConfig;
